{
  "name": "battle-war",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest",
    "lint": "npx @biomejs/biome lint  src/* tests/*",
    "format": "npx @biomejs/biome format --write src/* tests/*"
  },
  "devDependencies": {
    "@biomejs/biome": "2.0.6",
    "typescript": "5.8.3",
    "vite": "7.0.0",
    "vitest": "3.2.4"
  }
}
